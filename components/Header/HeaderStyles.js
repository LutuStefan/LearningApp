import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 50,
        // backgroundColor: 'red',
        position: "absolute",
        top: 50
    },
    headerImage: {
        width: 50,
        height: 50,
        position: "absolute",
        left: 10,
    }
});

export default styles;